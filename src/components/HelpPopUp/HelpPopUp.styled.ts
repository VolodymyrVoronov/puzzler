import styled from "styled-components";

const HelpPopUpContainer = styled.div`
  position: absolute;
  display: flex;
  justify-content: center;

  width: 1212px;

  margin: 0 auto;
`;

const HelpPopUpImage = styled.img`
  display: block;

  width: 100%;
`;

export { HelpPopUpContainer, HelpPopUpImage };
